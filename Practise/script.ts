// Typescript file to be used
// Named function
function add(x, y)
{
    return x + y;
}
// Anonymous function
let myAdd = function (x, y)
{
    return x + y;
};

const result = myAdd(10, 10);
console.log("Function result: " + result);
// alert(result);
