"use strict";
// Typescript file to be used
// Named fucntion
function add(x, y) {
    return x + y;
}
;
// Anonymous function
var myAdd = function (x, y) {
    return x + y;
};
var result = myAdd(10, 10);
console.log("Function result: " + result);
// alert(result);
